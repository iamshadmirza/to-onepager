# to-onepager config
# Copy to config.md next to SKILL.md and fill in.
# Local to your install. Do not commit to a public skill repo.

destination: local

# local — Markdown output directory (workspace-relative or absolute)
local_dir: one-pagers/

# google-docs — optional folder URL; leave empty to ask at publish time
google_docs_folder_url:

# confluence — folder or space URL where pages are created
confluence_folder_url:

# confluence — site origin for people-profile links in local Markdown copies
# Example: https://example.atlassian.net
atlassian_site:
